<% content_for :title, t('projects.edit.title') %>

<div class="page-header">
  <div class="container">
    <div>
      <div class="text-sm text-muted mb-1"><%= link_to @project.name, project_path(@project) %> /</div>
      <h1 class="page-title"><%= t('projects.edit.title') %></h1>
    </div>
    <div class="page-actions">
      <%= link_to t('actions.back'), project_path(@project), class: 'btn btn-outline' %>
    </div>
  </div>
</div>

<div class="page-content">
  <div class="container-sm">
    <div class="form-card">
      <%= form_with model: @project, local: true do |f| %>
        <%= render 'form', f: f %>
        <hr class="divider">
        <div class="flex gap-3">
          <%= f.submit t('helpers.submit.update', model: 'Project'), class: 'btn btn-amber' %>
          <%= link_to t('actions.cancel'), project_path(@project), class: 'btn btn-outline' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
